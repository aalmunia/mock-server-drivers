{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "The server (script) to return the data structs for the planning application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "randomstring": "^1.1.5",
    "sqlite3": "4.0.2"
  },
  "devDependencies": {
    "body-parser": "^1.18.3",
    "sqlite-sync": "^0.3.8"
  }
}
